# Model access for the discovery run (replay must work without this set).
ANTHROPIC_API_KEY=

# Target surface the agent is allowed to drive.
TARGET_BASE_URL=

# Comma-separated allowlist of permitted origins. The agent must not act outside it.
ALLOWED_ORIGINS=
